# Force x86_64 platform to match GitHub Actions runner
FROM --platform=linux/amd64 mcr.microsoft.com/devcontainers/base:ubuntu-24.04

# The rest is handled by the base image and devcontainer.json features