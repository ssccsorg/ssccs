"""
C2PA signing (WIP — was _utils/sign_c2pa.py).
"""

import logging

logger = logging.getLogger(__name__)


def sign_pdf(pdf_path: str, c2pa_manifest: str | None = None) -> bool:
    """Apply C2PA signature to a PDF. (stub)"""
    return True
