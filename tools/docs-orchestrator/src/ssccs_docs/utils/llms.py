"""
LLMS generation (WIP — was _utils/generate_llms_txt.py).
"""

import logging

logger = logging.getLogger(__name__)


def generate_llms_txt(docs_root: str) -> bool:
    """Generate llms.txt and per-page .llms.md files. (stub)"""
    return True
