---
title: "My Documentation"
subtitle: "Generated with SDBS"
date: last-modified
metadata-files:
  - _include/author.yml
abstract: |
  Placeholder abstract. Edit this file to document your project.
---

{{< include _include/_title_meta_items.qmd >}}

```{python}
#| include: false
#| context: local
%run _include/_graphviz.py
```

## Getting Started

Replace this content with your documentation.

### Adding Documents

Create `.qmd` files in this directory. Each file with a YAML front matter block
becomes a build target. Target names are derived from the relative path with
path separators replaced by hyphens:

- `docs/index.qmd` becomes `index`
- `docs/guide/index.qmd` becomes `guide-index`

### Building

```bash
# Build all targets
ssccs-docs build .

# Build a single target
ssccs-docs build . index

# Build with website profile (parallel)
ssccs-docs build . --website -j 4
```
